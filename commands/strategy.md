---
description: List strategies, show details of one, or stage one for trade-cycle to use
argument-hint: [strategy-name] | details <name> | create
---

Run the `strategy` skill with the argument in $ARGUMENTS.

- No argument → list all strategies from `strategies/` folder
- A strategy name → show that strategy's full rules
- `create` → walk the user through drafting a new strategy file
